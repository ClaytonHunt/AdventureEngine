# AdventureEngine — environment variable template
# Copy to .env.local for local development. Never commit filled-in env files.
# Use placeholders only in this template — never real tokens, passwords, or connection strings.

# ---------------------------------------------------------------------------
# API — ASP.NET Core
# ---------------------------------------------------------------------------

# CORS origins allowed by the API. Add one entry per array index.
AllowedOrigins__0=http://localhost:5173
# AllowedOrigins__1=https://staging.example.invalid

# ASP.NET Core environment name.
ASPNETCORE_ENVIRONMENT=Development

# ASP.NET Core HTTP bind settings.
ASPNETCORE_URLS=http://localhost:7001
ASPNETCORE_HTTP_PORTS=7001

# ---------------------------------------------------------------------------
# Web — Vite / React
# ---------------------------------------------------------------------------

# Public API base URL used by the frontend client bundle.
# NOTE: VITE_* values are exposed client-side; never place secrets in VITE_*.
VITE_API_BASE_URL=https://localhost:7001
