# AdventureEngine — environment variable reference
# Copy this file to .env.local and fill in values for your environment.
# .env.local is gitignored and should NEVER be committed.
#
# In local development, most of these are already set via launchSettings.json
# (apps/Api/Properties/launchSettings.json) and do not need to be set manually.

# ---------------------------------------------------------------------------
# API — ASP.NET Core
# ---------------------------------------------------------------------------

# CORS: Origins the API will accept cross-origin requests from.
# ASP.NET Core env-var array indexing: AllowedOrigins__0, AllowedOrigins__1, etc.
# In local dev, this is set automatically via launchSettings.json.
AllowedOrigins__0=http://localhost:5173
# AllowedOrigins__1=https://your-staging-domain.example.com

# ASP.NET Core environment (Development | Staging | Production)
ASPNETCORE_ENVIRONMENT=Development

# ---------------------------------------------------------------------------
# Web — Vite / React
# ---------------------------------------------------------------------------

# Base URL the React app uses to reach the API.
# NOTE: This value is bundled into the client-side JS — do NOT put secrets here.
VITE_API_BASE_URL=https://localhost:7001
