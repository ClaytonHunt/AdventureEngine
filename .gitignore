# Node
node_modules/
dist/
build/
.turbo/

# pnpm
.pnpm-store/

# Environment
.env
.env.local
.env.*.local
!.env.example

# .NET
bin/
obj/
*.user
*.suo
.vs/
TestResults/
*.trx

# .NET User Secrets (never commit)
# Windows: %APPDATA%\Microsoft\UserSecrets
# macOS/Linux: ~/.microsoft/usersecrets

# ASP.NET Core / Aspire
appsettings.Development.json
appsettings.*.json
!appsettings.json
launchSettings.json
# The AppHost launchSettings.json is not a secret — it contains only localhost ports
# required for the Aspire dashboard to start. It must be committed so `dotnet run` works
# on any developer machine without a manual setup step.
!apps/AppHost/Properties/launchSettings.json
# The Api launchSettings.json sets ASPNETCORE_ENVIRONMENT=Development so that health
# endpoints and OpenAPI/Scalar UI are active when running the API standalone (without Aspire).
!apps/Api/Properties/launchSettings.json
.aspire/

# NuGet packages — packages.lock.json files MUST be committed (used for CI --locked-mode)
*.nupkg

# macOS
.DS_Store
.AppleDouble
.LSOverride

# JetBrains
.idea/
*.iml

# VS Code (keep shared config templates, ignore personal settings)
.vscode/
!.vscode/extensions.json
!.vscode/launch.json.template
!.vscode/settings.json

# Playwright
playwright-report/
test-results/

# Test coverage output
coverage/
